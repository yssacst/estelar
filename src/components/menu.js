import '../css/menu.css';

const Menu = () => {
    return(
        <nav className="nav-menu">
            <a href="#home">HOME</a>
            <a href="#loja">LOJA</a>
            <a href="#contato">CONTATO</a>
        </nav>
    );
}

export default Menu;